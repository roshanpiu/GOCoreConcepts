package main 

import "fmt"

func main() {
    //from format package use println function
    
    //go convension - exported functions or variables from a package begins with a uppercase letter ex-'Println()' 
    //go convension - private functions and variables begins with a lowercase letter 
    
    //we can name the go file what ever we want the package name at the begining determines the name of the package
    
    //go is unicode ready (we can use multiple languages which has different characters)
    
    fmt.Println("Hello World")
}